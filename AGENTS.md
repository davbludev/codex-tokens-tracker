## Instruction file synchronization

Keep root `AGENTS.md` and `CLAUDE.md` identical. Whenever either file changes, apply the same change to the other in the same edit and verify their contents match.

## Agent skills

### Issue tracker
Before tracker operations, read `docs/agents/issue-tracker.md`.

### Triage labels
Before triaging, read `docs/agents/triage-labels.md`.

### Domain docs
Before exploring the codebase, read `docs/agents/domain.md`.
