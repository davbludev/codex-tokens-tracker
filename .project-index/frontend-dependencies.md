# Frontend dependencies

## Frontend packages
- Owner: `package.json`, `package-lock.json`.
- Responsibility: Declare frontend scripts and npm dependencies, including direct uPlot for dashboard chart rendering, and lock resolved package versions.
- Look here when: Changing frontend dependencies or finding the production build command.
